{
	"name": "wasm-bindgen-test",
	"version": "0.1.0",
	"description": "Test runner for WASM bindings",
	"type": "module",
	"scripts": {
		"build": "wasm-pack build --target web --out-dir pkg",
		"test": "wasm-pack test --node"
	},
	"devDependencies": {
		"wasm-pack": "^0.12.1"
	}
}
