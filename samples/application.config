# Database Configuration database.host=localhost database.port=5432 database.name=myapp database.username=admin database.password=secret123 database.ssl.enabled=true database.pool.minSize=5 database.pool.maxSize=20

# Server Configuration server.port=8080 server.host=0.0.0.0 server.compression.enabled=true server.request.timeout=30000

# Logging Configuration logging.level=INFO logging.file.enabled=true logging.file.path=/var/log/myapp.log logging.console.enabled=true

# Feature Flags features.authentication=true features.analytics=false features.newUI=true features.beta.enabled=false

# Cache Configuration cache.redis.host=localhost cache.redis.port=6379 cache.redis.database=0 cache.ttl=3600
